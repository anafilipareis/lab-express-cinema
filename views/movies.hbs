{{#each movies}}
    <div>
      <img src="{{image}}" alt="{{title}}" />
      <h2>{{title}}</h2>
      <a href="/movies/{{_id}}">See more</a>
    </div>
    {{/each}}

    {{!-- Não me está a aparecer os filmes na página. --}}